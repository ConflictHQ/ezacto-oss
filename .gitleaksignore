# Reviewed findings that are not secrets. One line per fingerprint, each with the
# reason — an unexplained entry here is indistinguishable from a covered-up leak.
#
# Fingerprints are commit:path:rule:line, so an entry stops applying the moment the
# line moves. That is deliberate: a re-review is cheap, a stale blanket exemption is
# not. Never add a path glob here — a rule that cannot see test files cannot catch
# the real secret somebody eventually pastes into one.
#
# NOTE: describe offending values, never reproduce them. A comment quoting the
# literal string re-introduces it in a new commit, and the scanner is right to
# flag that too.

# A CLI test fixture: the placeholder assigned to HARVEST_PAT was 17 characters,
# which trips the {16,} length heuristic in .gitleaks.toml. Not a credential —
# real Harvest PATs are 96 characters, shaped <digits>.pt.<random>. Fixed forward
# so the current fixture is four characters; the historical occurrence is pinned
# here because the full history is scanned.
c39dd69f59d2baddb8a7108b7269badbb93d22e3:packages/migrate/test/cli.test.ts:harvest-pat-assignment:79
# The same reviewed fixture again, re-attributed by history traversal to the
# extract-engine merge. The repoint dropped this one on the reading that the
# rewrite had collapsed the re-attribution; it did not, so it is pinned back at
# the merge's new fingerprint rather than left to fail every branch scan.
08833a4ad443e3ecdc11caca8d971a7e631085dc:packages/migrate/test/cli.test.ts:harvest-pat-assignment:91

# A historical test-only synthetic JWT fixture; current source constructs the
# detector-shaped value from separate segments.
8509982365da8ec0c84f4da3eb9ecad6e2c2d830:packages/migrate/test/worksheets.test.ts:jwt:543

# Mailgun provider test fixtures: test-only placeholder API key values used to
# verify the HTTP Authorization header construction. Not real credentials.
89fb16f8dea1c97a5ec5009c066326cb71811f2a:packages/mailer/test/mailgun.test.ts:generic-api-key:5
89fb16f8dea1c97a5ec5009c066326cb71811f2a:packages/mailer/test/mailgun.test.ts:generic-api-key:57

# An SSO domain-verification challenge token in a test. A challenge token is
# written by the server from random bytes and is then published by the domain
# owner in a public TXT record to prove ownership, so a value of this shape is
# not authentication material by design — publishing it is the whole mechanism.
# Migration 0039_sso_provisioning_domains.ts documents that, and the domain in
# the test is a reserved `.test` name. Current source composes the value at
# runtime; the historical occurrence is pinned because history is scanned.
96607e35f2f4069469b000389f91341bcabd0fdc:packages/api/test/sso-domains.test.ts:generic-api-key:28
b1a775fa97d6f916bc03946ea32c876d6198d37b:packages/api/test/sso-domains.test.ts:generic-api-key:28
