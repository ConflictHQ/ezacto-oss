# Reviewed findings that are not secrets. One line per fingerprint, each with the
# reason — an unexplained entry here is indistinguishable from a covered-up leak.
#
# Fingerprints are commit:path:rule:line, so an entry stops applying the moment the
# line moves. That is deliberate: a re-review is cheap, a stale blanket exemption is
# not. Never add a path glob here — a rule that cannot see test files cannot catch
# the real secret somebody eventually pastes into one.
#
# NOTE: describe offending values, never reproduce them. A comment quoting the
# literal string re-introduces it in a new commit, and the scanner is right to
# flag that too.

# A CLI test fixture: the placeholder assigned to HARVEST_PAT was 17 characters,
# which trips the {16,} length heuristic in .gitleaks.toml. Not a credential —
# real Harvest PATs are 96 characters, shaped <digits>.pt.<random>. Fixed forward
# in the same commit that adds this line (the fixture is now four characters);
# history is not rewritten, so the historical occurrence is pinned here.
8a37092d61a3c8df18e274ced30d6a4cb4f6136e:packages/migrate/test/cli.test.ts:harvest-pat-assignment:79

# An invoice-import mutation-guard fixture uses a syntactically valid product
# reference token. It is neither an API key nor accepted by any authentication
# path; the historical occurrence is pinned because the branch history is scanned.
# Git 2.55 also reports the unchanged fixture at the later transform/load and
# idempotent-load merges, where history traversal re-attributed the same line.
# The #198 worksheet integration merge re-attributed it once more.
# These exact reviewed fingerprints are retained because the value is not
# authentication material. The earlier extract-engine merge similarly
# re-attributed the reviewed short CLI placeholder; its exact merge fingerprint
# is retained as well.
30d0d8781fa36f41f0501d0da65b055833d9db3f:packages/db/test/invoice-import-contract.test.ts:generic-api-key:628
15234222dddd2bc9683189346769cabffdb83b64:packages/db/test/invoice-import-contract.test.ts:generic-api-key:646
2f178a98e8ef6dee345ed4755cb20b790311f687:packages/db/test/invoice-import-contract.test.ts:generic-api-key:646
ce42034359565ae94f14a77dae23c29fa801b4a0:packages/db/test/invoice-import-contract.test.ts:generic-api-key:646
8b0372c89ab678f84025d4771932bd5224ef81ef:packages/db/test/invoice-import-contract.test.ts:generic-api-key:646
6f78aa1c996190ab07fa311d8c394cae72a48f9d:packages/db/test/invoice-import-contract.test.ts:generic-api-key:646
41246407df66dcf3ea2d555acfe6bb5545a5bfc8:packages/migrate/test/cli.test.ts:harvest-pat-assignment:91

# A historical test-only synthetic JWT fixture; current source constructs the
# detector-shaped value from separate segments.
a7f98dc929b3dc4d98abe951e6b0cc9c9df26aeb:packages/migrate/test/worksheets.test.ts:jwt:543

# Mailgun provider test fixtures: test-only placeholder API key values used to
# verify the HTTP Authorization header construction. Not real credentials.
2c6b98a95969c24cb6177f12fd0436d3a0ef7e99:packages/mailer/test/mailgun.test.ts:generic-api-key:5
2c6b98a95969c24cb6177f12fd0436d3a0ef7e99:packages/mailer/test/mailgun.test.ts:generic-api-key:57
6b76ed5547937059a37566ab41cbf5b624630267:packages/mailer/test/mailgun.test.ts:generic-api-key:5
6b76ed5547937059a37566ab41cbf5b624630267:packages/mailer/test/mailgun.test.ts:generic-api-key:57

# An SSO domain-verification challenge token in a test. A challenge token is
# written by the server from random bytes and is then published by the domain
# owner in a public TXT record to prove ownership, so a value of this shape is
# not authentication material by design — publishing it is the whole mechanism.
# Migration 0039_sso_provisioning_domains.ts documents that, and the domain in
# the test is a reserved `.test` name. Current source composes the value at
# runtime; the historical occurrence is pinned because history is scanned.
1e9ce7f9e132700dc8a5a3dc01f25da9b3e7561d:packages/api/test/sso-domains.test.ts:generic-api-key:28
