# ezacto-oss — local dev secrets (copy to .dev.vars; NEVER commit .dev.vars)
# Infrastructure bindings (D1 database, R2 receipts/exports, Queues) are wrangler
# bindings, not env vars — they live in wrangler.jsonc when the worker entry lands.

# --- Identity seam: Cloudflare Access implementation (D4) ---
ACCESS_TEAM_DOMAIN=https://<team>.cloudflareaccess.com
ACCESS_POLICY_AUD=

# --- Mailer seam: Mailgun implementation (D14) ---
# HTTP-first, which is what the Worker target requires (Workers cannot SMTP).
# Sending domain is a subdomain so app mail and human mail do not share one
# DKIM/DMARC surface: go.<domain> sends, the apex is Google Workspace.
MAILGUN_API_KEY=
MAILGUN_SENDING_DOMAIN=go.ezacto.com
MAILGUN_REGION=us
MAIL_FROM_DOMAIN=go.ezacto.com

# --- App ---
APP_BASE_URL=http://localhost:8787
SESSION_SECRET=

# --- migrate package (dev, against the live Harvest account) ---
HARVEST_ACCOUNT_ID=
HARVEST_PAT=
HARVEST_USER_AGENT_EMAIL=
